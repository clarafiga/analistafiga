- serve para comentar

CREATE DATABASE teste_ecommerce; -- O PONTO E VIRGULA ENCERRA LINHA	

USE teste_ecommerce;

-- criando a entidade produtos

CREATE TABLE Produtos (
id_prod VARCHAR(10),  -- TEXT SE COLOCAR NUMERO ENTRE PARENTESES VIRA UM LIMITE 
nome VARCHAR (100),
categoria VARCHAR (50),
preco DECIMAL (8,2),               -- CAN BE TINY SMALL BIG
estoque INT 
);


-- REMOÇAO DE TABELAS CONTEU 
DROP TABLE Produtos;




-- id_produto,nome,categoria,preco,estoque

-- 'OPT_LOCAL_INFILE=1' -- (passo extra 02 junto ao load data) inserir na sua conexão local (edit da conexão >> Advanced >> Others)

LOAD DATA LOCAL INFILE 'C:\Users\clara.figueredo\Documents\bigdata\vendas_produtos(in).csv' -- Ajuste o caminho no seu banco local
INTO TABLE teste_ecommerce.produtos
FIELDS TERMINATED BY ',' 
ENCLOSED BY '"'
LINES TERMINATED BY '\r\n' -- Aqui: CR LF
IGNORE 1 ROWS -- Pula o cabeçalho 'id_produto,nome...'
(id_produto, nome, categoria, @preco_var, estoque) -- Mapeia colunas
SET preco = REPLACE(@preco_var, '.', '.'); -- Garante que o decimal seja lido corretamente
